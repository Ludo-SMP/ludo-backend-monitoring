apiVersion: 1

providers:
  - name: Prometheus
    folder: Dashboards
    type: file
    disableDeletion: true
    updateIntervalSeconds: 5
    options:
      path:
        "/var/lib/grafana/dashboards"